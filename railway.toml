[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["packages/backend/**", "Dockerfile"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.healthcheck]]
httpPath = "/health"
port = 4100

[[deploy.healthcheck]]
httpPath = "/health"
port = 8080
